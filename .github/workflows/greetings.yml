name: Greetings

on:
  pull_request_target:
    types: [opened]
  issues:
    types: [opened]

permissions:
  issues: write
  pull-requests: write

jobs:
  greeting:
    runs-on: ubuntu-latest
    steps:
      - name: Greet first-time contributors (first-interaction)
        uses: actions/first-interaction@v1
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          issue-message: >
            👋 Hi there! Thanks for opening your first issue in **snowflake-handbook** 🎉  
            We appreciate your contribution. Please provide as much detail as possible so we can help.
          pr-message: >
            🚀 Thanks for submitting your first pull request to **snowflake-handbook** 🎉  
            We'll review it soon. Meanwhile, please ensure you followed the contribution guidelines.
